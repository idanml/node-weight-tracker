pipeline {
    agent {label 'VM1slave'}
    stages {
        stage('Create .env') {
            steps {
                sh "echo 'Hello World'
				echo 'Hello World2'"
            }
        }
    }
}
